pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code pulled from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }

        stage('Test') {
            steps {
                echo 'Tests running'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment done'
            }
        }
    }
}
